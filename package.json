{
  "name": "devseek-api",
  "version": "1.0.0",
  "description": "Mock API for Devseek project using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch jobs.json --"
  },
  "dependencies": {
    "json-server":"^0.17.4"
  }
}
