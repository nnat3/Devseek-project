{
  "name": "devseek-api",
  "version": "1.0.0",
  "description": "Mock API for Devseek project using JSON Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch jobs.json --port 10000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server":"^0.17.4"
  }
}
